# Rules

- **Git push control**: Do not run `git push` or push changes to the remote Git repository unless the user explicitly commands it. Staging (`git add`) and committing (`git commit`) is allowed, but pushing changes must be requested explicitly by the user.
